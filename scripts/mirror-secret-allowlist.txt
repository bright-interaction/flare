# Substrings exempted from the mirror pre-flight secret scan
# (scripts/mirror-secret-preflight.sh). One per line. A history line containing
# any of these is not treated as a credential.
#
# Only add a value that is provably synthetic, and say why. Anything real must be
# purged from history instead, never allowlisted.

# internal/api/secret_column_test.go and internal/secretbox/secretbox_oracle_test.go.
# The encryption-oracle tests need a value that LOOKS like a provider key, because
# the bug they cover was Flare decrypting attacker-supplied ciphertext back out.
# The value is the literal words "provider key".
sk-live-provider-key

# internal/secretbox/secretbox_oracle_test.go. Stands in for the victim tenant's
# stored credential in the oracle test. The value spells out what it is.
ghp_victim_token_do_not_leak

# internal/api/mcp_metrics_scrub_test.go and internal/api/sensitive_test.go.
# AWS's own documented example access key id, published in their docs; the
# EXAMPLE suffix is reserved for exactly this. Flare's whole feature here is
# detecting leaked credentials in event payloads, so the fixture has to look
# like a real key or the test proves nothing.
#
# This entry is the second half of a fix that only landed on one twin. The same
# value was already exempted in flare/.gitleaks.toml, so someone had established
# it was synthetic, but the mirror pre-flight keeps its OWN allowlist and never
# got the entry. Result: flare's public mirror sat unpublishable while the
# adjacent scanner was green. Two allowlists over the same corpus is the estate's
# incomplete-migration shape; when you exempt a fixture, exempt it in both.
AKIAIOSFODNN7EXAMPLE

# Same shape again, one round later. flare/AUDIT-2026-08-11.md carries this
# synthetic value in a repro showing an attacker receiving a victim org's key.
# flare/.gitleaks.toml already allowlists it via its (?i)victim marker rule, so
# the authoritative gate is green, but this coarse pre-flight keeps its own list
# and would refuse the publish. The audit doc is stripped from the mirror
# entirely by the AUDIT-*.md glob in split-public-repo.sh, so the value never
# reaches the public repo either way; this entry just stops the pre-flight
# failing closed on a file that is about to be removed.
sk-VICTIM-ORG-KEY
